export default function AddMobileButton() {
	return (
		<button className='flex items-center justify-center w-16 h-10 text-base font-bold rounded-full md:hidden bg-brand text-title'>
			<p>+</p>
		</button>
	);
}
